---
## PARAMETER MANAGER CONFIG
param-efluid:

   managed-datasource:
       driverClassName: org.postgresql.Driver
       url: jdbc:postgresql://small-server/demo?searchpath=public
       username: user
       password: user
       query: 
           tableNamesProtected: true
           columnNamesProtected: true
   
   managed-updates:
       check-update-missing-ids: true
       check-delete-missing-ids: true
           
   details:
       version: 1.1.1

## TECH FEATURES CUSTOM
spring:
   profiles:
       active: demo

   thymeleaf:
       cache: false

   datasource:
       url: jdbc:postgresql://small-server/manager
       username: user
       password: user
       driver-class-name: org.postgresql.Driver

   jpa:
       show-sql: false
       hibernate:
           ddl-auto: create

## WEB SERVER CONFIG
server:
   contextPath:
   port: 8080